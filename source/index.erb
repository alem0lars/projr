<!DOCTYPE html>
<!--[if lt IE 7]>      <html class="en lt-ie9 lt-ie8 lt-ie7"> <![endif]-->
<!--[if IE 7]>         <html class="en lt-ie9 lt-ie8"> <![endif]-->
<!--[if IE 8]>         <html class="en lt-ie9"> <![endif]-->
<!--[if gt IE 8]><!--> <html class="en"> <!--<![endif]-->

  <head>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">

    <title><%= v[:title] %></title>

    <meta name="author" content="<%= v[:author][:name] %>">
    <meta name="description" content="<%= v[:description] %>">

    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <script data-main="scripts/app.js" src="scripts/lib/require.js"></script>

    <link rel="stylesheet" href="<%= v[:styles_name] %>/app.css">

    <link rel="shortcut icon" href="<%= v[:images_name] %>/icons/favicon.ico">

    <% if v[:images_pth].join('icons', 'apple-touch-icon.png').exist? %>
      <%= "<link rel=\"apple-touch-icon\" href=\"#{v[:images_name]}/icons/apple-touch-icon.png\">" %>
    <% end %>
    <% if v[:images_pth].join('icons', 'apple-touch-icon-72x72.png').exist? %>
      <%= "<link rel=\"apple-touch-icon\" href=\"#{v[:images_name]}/icons/apple-touch-icon-72x72.png\">" %>
    <% end %>
    <% if v[:images_pth].join('icons', 'apple-touch-icon-114x114.png').exist? %>
      <%= "<link rel=\"apple-touch-icon\" href=\"#{v[:images_name]}/icons/apple-touch-icon-114x114.png\">" %>
    <% end %>

  </head>

  <body>

    <!--[if lt IE 7]>
      <p class="chromeframe">
        You are using an <strong>outdated</strong> browser.
        Please <a href="http://browsehappy.com/">upgrade your browser</a> or
        <a href="http://www.google.com/chromeframe/?redirect=true">activate Google Chrome Frame</a>
        to improve your experience.</p>
    <![endif]-->

    <%= get_from_templ(v[:markup_pth].join('navigation.erb'), v) %>

    <div class="container-fluid">

      <header class="header-wrp row-fluid">
        <%= get_from_templ(v[:markup_pth].join('header.erb'), v) %>
      </header>

      <div class="main-wrp row-fluid">
        <%= get_from_templ(v[:markup_pth].join('main.erb'), v) %>
      </div>

      <footer class="footer-wrp row-fluid">
        <%= get_from_templ(v[:markup_pth].join('footer.erb'), v) %>
      </footer>

    </div>

  </body>

</html>
